spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://mysql:3306/smart_resume?characterEncoding=UTF-8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: your-db-password
    hikari:
      maximum-pool-size: 20
  data:
    redis:
      host: redis
      port: 6379
      database: 0
      lettuce:
        shutdown-timeout: 2000ms

app:
  frontend-url: https://your-domain.com
  mail:
    from-name: SmartResume 智能简历
