# AI 简历生成与优化系统 — 需求文档

> 基于 Vue 3 + Vite + Spring Boot 3.2.5 + Spring AI + MySQL + Redis + Chroma

---

 按模块拆解评估：

  工作量评估

  ┌─────────────────────────────────────┬────────┬──────────┬───────────────────────────────────────────────────┐
  │                模块                 │ 复杂度 │ 预估耗时 │                       说明                        │
  ├─────────────────────────────────────┼────────┼──────────┼───────────────────────────────────────────────────┤
  │ 数据库建表 + 后端 CRUD              │ 中     │ 2-3 天   │ 7 张表，User/Resume/Content/Version/Share/Chat/Notice │
  ├─────────────────────────────────────┼────────┼──────────┼───────────────────────────────────────────────────┤
  │ 前端简历编辑页（表单+预览）         │ 高     │ 3-4 天   │ 核心页面，TipTap(Vue)集成，实时同步逻辑            │
  ├─────────────────────────────────────┼────────┼──────────┼───────────────────────────────────────────────────┤
  │ Spring AI 接入 + AI 对话生成（SSE） │ 高     │ 3-5 天   │ 最大的不确定性，GLM 兼容性、流式响应、Prompt 调优 │
  ├─────────────────────────────────────┼────────┼──────────┼───────────────────────────────────────────────────┤
  │ AI 润色优化                         │ 中     │ 1-2 天   │ 复用 AI 层，主要是 Prompt 设计                    │
  ├─────────────────────────────────────┼────────┼──────────┼───────────────────────────────────────────────────┤
  │ 前端 AI 对话页（自定义组件）         │ 中     │ 2-3 天   │ 聊天气泡/输入框/会话列表 集成                      │
  ├─────────────────────────────────────┼────────┼──────────┼───────────────────────────────────────────────────┤
  │ 简历模板（3 套）                    │ 中     │ 2-3 天   │ 工作量不大但繁琐，CSS 排版调细节                  │
  ├─────────────────────────────────────┼────────┼──────────┼───────────────────────────────────────────────────┤
  │ Chroma 接入 + 长期记忆              │ 中高   │ 3-4 天   │ 嵌入式 Chroma 相对简单，但向量化和检索逻辑要设计  │
  ├─────────────────────────────────────┼────────┼──────────┼───────────────────────────────────────────────────┤
  │ Redis 缓存 + 限流                   │ 低     │ 1 天     │ 草稿保存、对话缓存、限流                          │
  ├─────────────────────────────────────┼────────┼──────────┼───────────────────────────────────────────────────┤
  │ 主动服务定时任务                    │ 中     │ 2 天     │ Scheduler + Prompt + 消息页                       │
  ├─────────────────────────────────────┼────────┼──────────┼───────────────────────────────────────────────────┤
  │ 岗位匹配度分析                      │ 中     │ 2 天     │ Prompt 设计 + 前端展示                            │
  ├─────────────────────────────────────┼────────┼──────────┼───────────────────────────────────────────────────┤
  │ 简历分享 + 二维码                   │ 低     │ 1-2 天   │ 公开页 + 密码 + 统计                              │
  ├─────────────────────────────────────┼────────┼──────────┼───────────────────────────────────────────────────┤
  │ PDF 导出                            │ 低     │ 0.5-1 天 │ html2pdf.js 集成                                  │
  ├─────────────────────────────────────┼────────┼──────────┼───────────────────────────────────────────────────┤
  │ AI 自我介绍                         │ 低     │ 1 天     │ Prompt + 简单页面                                 │
  ├─────────────────────────────────────┼────────┼──────────┼───────────────────────────────────────────────────┤
  │ 联调 + 测试 + UI 打磨               │ 中     │ 3-5 天   │ 端到端调试                                        │
  └─────────────────────────────────────┴────────┴──────────┴───────────────────────────────────────────────────┘

  汇总

  - 纯编码量：约 25-35 天（全职投入）
  - 但实际瓶颈不是写代码，而是：

  1. Spring AI + GLM 兼容性调试 — 最大不确定性，如果踩坑可能多花几天
  2. Chroma 向量化方案设计 — 怎么切分文本、用什么 embedding 模型需要试
  3. GLM-5 生成质量 — Prompt 调优是反复迭代的过程，AI 写代码快但调 Prompt 慢
  4. 前后端联调 — SSE 流式、AI 对话状态同步等容易出 bug

  结论

  如果全职投入（每天 6-8 小时），用 Claude Code + GLM-5：最快 4-5 周可以完成全部功能（含测试打磨）。

## 一、前端（Vue 3 + Vite）

### 技术栈

| 类别 | 技术 | 说明 |
|------|------|------|
| UI 组件 | `Element Plus`（新增） | 表单、表格、布局等 |
| 长文本编辑 | `@tiptap/vue-3`（新增） | 项目经历描述、自我评价等模块 |
| PDF 导出 | `html2pdf.js`（新增） | 从预览 DOM 直接生成 PDF |
| 二维码生成 | `vue-qrcode`（新增） | 简历分享二维码 |

### 编辑页交互方案：表单 + 可交互预览

  ┌─────────────────┬──────────────────────────┐
  │   el-form 表单   │      简历实时预览          │
  │                 │                          │
  │ [姓名] 张三     │  ┌────────────────────┐  │
  │ [邮箱] xxx@xx   │  │  张三              │  │
  │ [技能] Java...  │  │  xxx@xx.com        │  │
  │                 │  │                    │  │
  │ [+ 添加项目经历] │  │  项目经历            │  │
  │ [项目名] XXX    │  │  · XXX项目          │  │
  │ [描述] 点击编辑  │  │    描述文字...      │  │
  │                 │  │                    │  │
  └─────────────────┴──┴────────────────────┴──┘

- 左侧 el-form：结构化输入，AI 拿到的就是干净的 JSON
- 右侧预览：自定义 Vue 组件渲染，表单任何改动实时同步到预览
- AI 润色：选中预览中某段文字 → 弹出 AI 对话 → 润色结果回填到表单 → 预览自动更新


 resumeData (统一 JSON)
      ├── <ClassicTemplate :data="resumeData" />   经典单栏
      ├── <DualColumnTemplate :data="resumeData" /> 双栏专业
      └── <CreativeTemplate :data="resumeData" />   创意现代

  切换时只是换渲染组件，数据不动：

  const templates = {
    classic: ClassicTemplate,
    dual: DualColumnTemplate,
    creative: CreativeTemplate,
  };

  const CurrentTemplate = templates[currentStyle];

  每个模板组件内部自己决定怎么排版（单栏/双栏、颜色、字体、间距），但读取的字段名完全一致（name、education、experience...）。

  新加模板 = 新加一个组件 + 注册到 templates 对象里，不影响其他任何代码。预览和 PDF 导出都用同一套组件/数据。

### 组件对应表

| 页面 | 组件 | 来源 |
|------|------|------|
| AI 对话生成 | 自定义 ChatBubble + ChatInput + ConversationList | 自己写 |
| 简历编辑-表单侧 | el-form + el-input/el-textarea/el-select | `Element Plus` |
| 简历编辑-长文本 | TipTap Editor | `@tiptap/vue-3` |
| 简历编辑-预览侧 | 自定义 Vue 组件 | 自己写 |
| 简历管理列表 | el-table / el-card | `Element Plus` |
| 岗位匹配度 | el-progress + el-card | `Element Plus` |
| 简历分享 | el-dialog + el-form + Qrcode | `Element Plus` + `vue-qrcode` |
| 模板选择 | el-segmented + 缩略图卡片 | `Element Plus` |

---

### 1. 简历对话生成页
- 对话式界面，用户通过聊天输入零散个人信息（教育背景、项目经历、技能等），AI 解析后生成结构化简历
- 支持用户在对话中对 AI 生成的内容进行追问和修改
- 生成完成后自动跳转到简历编辑页，用户可手动微调

### 2. 简历编辑页
- 左侧表单 + 右侧实时预览的布局
- 表单模块：基本信息、教育背景、工作/实习经历、项目经历、技能特长、自我评价
- 每个模块支持增删改查（如多条经历、多个技能标签）
- 右侧实时渲染简历预览，所见即所得
- AI 润色按钮：选中某段文字后点击，AI 对话式润色优化
- 自动保存（草稿）

### 3. 简历模板切换
- 前端组件化实现多种简历模板（至少 3 种风格：经典简约、双栏专业、创意现代）
- 模板切换时实时预览，不丢失已填写内容
- 模板之间数据结构统一，切换无需重新填写

### 4. 简历管理页（我的简历）
- 简历列表页：展示用户所有简历版本，卡片式布局
- 支持简历的创建、编辑、复制、删除
- 显示简历创建时间、最后修改时间、状态（草稿/已完成）
- 简历版本历史：查看某份简历的历史版本，支持回滚

### 5. 岗位匹配度分析页
- 用户粘贴 JD（岗位描述）文本
- AI 分析简历与 JD 的匹配度，给出评分（0-100）及详细分析
- 匹配维度拆分：技能匹配、经历匹配、学历匹配、关键词覆盖
- 针对低分项给出具体优化建议

### 6. AI 自我介绍生成
- 基于已有简历内容，AI 自动生成面试自我介绍
- 支持选择时长（1分钟/3分钟/5分钟）
- 支持针对特定岗位生成定制版自我介绍
- 生成结果支持一键复制

### 7. 简历分享页
- 生成简历公开访问页（独立 URL）
- 访客无需登录即可在线查看简历
- 支持生成分享二维码
- 支持设置访问密码、过期时间
- 访问量统计

### 8. PDF 导出
- 前端从简历预览 DOM 节点直接生成 PDF（`html2pdf.js`），所见即所得
- 支持选择当前模板样式导出
- 如需在线分享场景，可将 PDF 上传至腾讯云 COS 存储文件 URL

---

## 二、后端

### 1. 简历 CRUD 接口
- `POST /api/resume/add` — 创建简历（草稿）
- `POST /api/resume/update` — 更新简历内容（各模块数据）
- `POST /api/resume/delete` — 删除简历（逻辑删除）
- `GET /api/resume/get/{id}` — 获取简历详情
- `POST /api/resume/list/page` — 分页查询当前用户的简历列表
- `POST /api/resume/my/list` — 获取当前用户所有简历（不分页，用于简历管理）

### 2. 简历版本管理接口
- `POST /api/resume/version/save` — 保存简历版本快照
- `GET /api/resume/version/list/{resumeId}` — 获取某简历的版本历史列表
- `POST /api/resume/version/rollback` — 回滚到指定版本

### 3. AI 对话生成接口
- `POST /api/resume/chat/generate` — 对话式生成简历（流式响应 SSE）
  - 请求：对话历史 + 用户最新输入
  - 响应：AI 解析用户信息，返回结构化简历 JSON + 对话文本
- `POST /api/resume/chat/polish` — AI 润色指定内容
  - 请求：待润色文本 + 上下文（所属模块、目标岗位等）
  - 响应：润色后的文本

### 4. 岗位匹配度分析接口
- `POST /api/resume/match` — 分析简历与 JD 的匹配度
  - 请求：简历 ID + JD 文本
  - 响应：总分 + 各维度评分 + 优化建议

### 5. AI 自我介绍生成接口
- `POST /api/resume/self-intro` — 生成自我介绍
  - 请求：简历 ID + 目标岗位（可选）+ 时长要求
  - 响应：生成的自我介绍文本

### 6. 简历分享接口
- `POST /api/resume/share/create` — 创建分享链接
  - 参数：简历 ID、是否设密码、过期时间
  - 返回：分享 URL + 二维码
- `GET /api/resume/share/view/{shareKey}` — 访客查看分享简历（无需登录）
- `POST /api/resume/share/verify` — 验证分享密码
- `GET /api/resume/share/stats/{shareKey}` — 分享访问量统计

### 7. 主动服务（定时任务）
- Spring Scheduler 定时任务：每天检查简历更新时间
  - 超过 30 天未更新的简历，生成提醒消息
  - 分析简历薄弱项（如项目经历为空、技能过少），生成优化建议
- `GET /api/resume/notice/list` — 获取当前用户的主动提醒消息列表
- `POST /api/resume/notice/read/{id}` — 标记提醒已读

---

## 三、数据库（MySQL）

### 1. user 表 — 用户表
| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT | 主键自增 |
| username | VARCHAR(64) | 用户名（唯一） |
| userAccount | VARCHAR(64) | 账号（唯一，用于登录） |
| userPassword | VARCHAR(128) | 密码（加密存储） |
| avatarUrl | VARCHAR(512) | 头像 URL |
| gender | TINYINT | 性别：0=女 1=男 |
| email | VARCHAR(128) | 邮箱 |
| phone | VARCHAR(20) | 手机号 |
| userRole | INT | 角色：0=普通用户 1=管理员 |
| status | INT | 状态：0=正常 1=禁用 |
| createTime | DATETIME | 创建时间 |
| updateTime | DATETIME | 更新时间 |
| isDelete | TINYINT | 逻辑删除 |

### 2. resume 表 — 简历主表
| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT | 主键自增 |
| userId | BIGINT | 所属用户 ID |
| title | VARCHAR(256) | 简历标题（如"Java开发-XX公司"） |
| status | INT | 状态：0=草稿 1=已完成 |
| currentTemplate | VARCHAR(64) | 当前使用的模板标识 |
| createTime | DATETIME | 创建时间 |
| updateTime | DATETIME | 更新时间 |
| isDelete | TINYINT | 逻辑删除 |

### 3. resume_content 表 — 简历内容表
| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT | 主键自增 |
| resumeId | BIGINT | 关联简历 ID |
| moduleType | VARCHAR(32) | 模块类型：basic/education/experience/project/skill/selfIntro |
| contentJson | TEXT | 该模块的 JSON 数据 |
| sortOrder | INT | 排序（同模块多条数据时） |
| createTime | DATETIME | 创建时间 |
| updateTime | DATETIME | 更新时间 |

### 4. resume_version 表 — 简历版本表
| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT | 主键自增 |
| resumeId | BIGINT | 关联简历 ID |
| versionNum | INT | 版本号 |
| snapshotJson | LONGTEXT | 简历完整快照 JSON |
| remark | VARCHAR(256) | 版本备注（如"优化了项目描述"） |
| createTime | DATETIME | 创建时间 |

### 5. resume_share 表 — 简历分享表
| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT | 主键自增 |
| resumeId | BIGINT | 关联简历 ID |
| userId | BIGINT | 分享者用户 ID |
| shareKey | VARCHAR(64) | 唯一分享标识（UUID） |
| password | VARCHAR(64) | 访问密码（可选，存 MD5） |
| expireTime | DATETIME | 过期时间（可选） |
| viewCount | INT | 访问次数 |
| createTime | DATETIME | 创建时间 |
| isDelete | TINYINT | 逻辑删除 |

### 6. resume_chat 表 — AI 对话记录表
| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT | 主键自增 |
| resumeId | BIGINT | 关联简历 ID |
| userId | BIGINT | 用户 ID |
| role | VARCHAR(16) | 角色：user/assistant |
| content | TEXT | 对话内容 |
| createTime | DATETIME | 创建时间 |

### 7. resume_notice 表 — 主动提醒消息表
| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT | 主键自增 |
| userId | BIGINT | 用户 ID |
| resumeId | BIGINT | 关联简历 ID |
| type | VARCHAR(32) | 类型：update_remind/optimize_suggest/completeness_check |
| title | VARCHAR(256) | 提醒标题 |
| content | TEXT | 提醒内容（AI 生成的建议） |
| isRead | TINYINT | 是否已读：0=未读 1=已读 |
| createTime | DATETIME | 创建时间 |

---

## 四、Redis

### 1. Session 存储
- Spring Session Redis，用户登录态管理

### 2. 简历草稿自动保存
- Key: `resume:draft:{userId}:{resumeId}`
- Value: 简历完整 JSON
- TTL: 7 天
- 用途：前端定时（如每 30 秒）将编辑中的简历保存到 Redis，防止意外丢失

### 3. AI 对话上下文缓存
- Key: `resume:chat:{resumeId}`
- Value: 对话历史 List（最近 N 轮）
- TTL: 2 小时
- 用途：缓存当前对话上下文，避免频繁查库；对话结束或简历保存时清除

### 4. 分享链接访问限流
- Key: `resume:share:rate:{shareKey}:{ip}`
- Value: 访问次数
- TTL: 1 分钟
- 用途：防刷访问量，每分钟同一 IP 同一分享链接最多 10 次计入

### 5. 接口限流
- Key: `rate:resume:ai:{userId}`
- Value: 请求计数
- TTL: 1 分钟
- 用途：AI 相关接口限流，防止用户短时间内大量调用（每用户每分钟 10 次）

---

## 五、LLM（Spring AI + 智谱 GLM）

### 1. Spring AI 基础接入
- 引入 `spring-ai-starter-openai`（智谱兼容 OpenAI API 格式）
- 配置智谱 GLM API 地址和 Key
- 封装统一的 AI 调用 Service（ChatClient / ChatModel）

### 2. AI 简历生成
- **Prompt 设计**：System Prompt 定义简历结构规范 + 解析规则
- 用户输入零散信息 → LLM 结构化抽取 → 输出 JSON 格式简历数据
- 支持流式输出（SSE），前端逐字展示生成过程
- 多轮对话：支持追问补充信息，AI 增量更新简历内容

### 3. AI 润色优化
- **Prompt 设计**：根据上下文（目标岗位、模块类型）定制润色策略
- 输入：原始文本 + 目标岗位 + 润色要求
- 输出：润色后文本 + 修改说明
- 支持多种润色模式：专业度提升、精简压缩、STAR 法则重写、针对性优化

### 4. 岗位匹配度分析
- **Prompt 设计**：将简历 + JD 一起输入，要求 LLM 按维度评分
- 输出结构化评分结果 JSON：总分、技能匹配分、经历匹配分、学历匹配分、关键词覆盖率、详细优化建议
- 结果缓存到 Redis（相同简历 + 相同 JD 缓存 24 小时）

### 5. AI 自我介绍生成
- **Prompt 设计**：基于简历内容 + 目标岗位 + 时长要求
- 输出：口语化自我介绍文本，段落清晰、重点突出

### 6. 主动服务 Prompt
- **简历完整性检查**：分析简历各模块填充情况，生成优化建议
- **薄弱项识别**：对比目标岗位常见要求，指出缺失项
- **更新提醒文案**：根据用户画像生成个性化提醒消息

---

## 六、向量数据库（Chroma 嵌入式）

### 1. 用户信息长期记忆
- 存储内容：用户画像信息（专业、技能、求职方向偏好）的向量化表示
- 用途：AI 生成简历时，自动从向量库检索用户历史偏好，实现个性化生成
- Key 设计：`user_profile:{userId}`

### 2. 简历版本语义检索
- 存储内容：每份简历的向量化摘要
- 用途：用户创建新简历时，可从历史简历中语义检索相关经历片段，提示用户复用
- Key 设计：`resume_history:{userId}:{resumeId}`

### 3. 主动服务 — 智能推荐
- 存储内容：JD 样本、岗位关键词库的向量化表示
- 用途：根据用户简历内容，语义匹配推荐适配的岗位方向、技能补充建议
- Key 设计：`job_match:{category}`

---

## 七、开发优先级（建议实施顺序）

| 阶段 | 内容 | 预计周次 |
|------|------|----------|
| 第一阶段 | 后端简历 CRUD + 数据库建表 + 前端简历编辑页（表单+预览） | 第 1-2 周 |
| 第二阶段 | Spring AI 接入智谱 GLM + AI 对话生成简历 + AI 润色 | 第 3-4 周 |
| 第三阶段 | 简历模板切换 + PDF 导出 | 第 5 周 |
| 第四阶段 | Chroma 接入 + 长期记忆 + 简历版本管理 | 第 6-7 周 |
| 第五阶段 | 主动服务定时任务 + 岗位匹配度分析 + Redis 缓存优化 | 第 8-9 周 |
| 第六阶段 | 简历分享 + AI 自我介绍 + 整体测试与 UI 打磨 | 第 10-12 周 |
